---
// filepath: c:\Users\User\Desktop\websites\EDIMcGill.github.io\src\components\learning\PronunciationTag.astro
interface Props {
	term: string
	pronunciation: string
	class?: string
}

const { term, pronunciation, class: className = '' } = Astro.props as Props
---

<span
	class={`inline-flex items-center gap-2 rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-sm text-slate-700 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-200 ${className}`}
>
	<strong>{term}</strong>
	<span class='text-slate-500 dark:text-slate-400'>/ {pronunciation} /</span>
</span>
